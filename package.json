{
  "name": "tjandpals-utilities",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cors:app:set": "node ./src/cors.js --app --set",
    "cors:app:get": "node ./src/cors.js --app --get",
    "cdn-sync:hard": "node ./src/cdn-sync.js --hard",
    "cdn-sync:soft": "node ./src/cdn-sync.js --soft"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-spawn": "^6.0.5",
    "shelljs": "0.8.3"
  }
}
